import RegisterClient from "./RegisterClient";

export const metadata = {
  title: "Book Store | Register",
  description: "Register your account",
};

const LoginPage = () => {
  return (
    <div>
      <RegisterClient />
    </div>
  );
};

export default LoginPage;
